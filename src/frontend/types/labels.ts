export type TLabelItem = {
    text: string,
};

export type TTooltipItem = {
    label: string,
    value: string,
    datasetIndex: number,
};

export type TLegendItem = {
    hidden: boolean,
    datasetIndex: number,
};
